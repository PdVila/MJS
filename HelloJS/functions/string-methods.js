let name = "Peter Vila";

//Length Property;
console.log(name.length); //It's not a method ,its a property

// Convert to Upper Case;
name.toUpperCase() //Converts string to upperCase

name.toLowerCase();

//Includes Method
let password = 'abc123asdf098'
console.log(password.includes('password'))

console.log(name.trim()); //Removes white space from ends of string
